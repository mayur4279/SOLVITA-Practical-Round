### Docker file has been created.   

#### Step-1: We need to create the image from Dockerfile 

`docker build -t . ` 

#### Step-2: we can run our image using  docker CLI or using docker compose.   

`docker run -d -p 8080:80 <image-name> --name <container-name>`  

`docker-compose up`  

#### Docker & Containerisation Section has been completed.



